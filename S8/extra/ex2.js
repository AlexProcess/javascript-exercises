async (openPlanets => openPlanets);{
    const res = await fetch (`http://localhost:3000/diary`);
    const resPlan = await res.json();
    console.log(resPlan) 
}

for (let i = 0; i < array.length; i++) {
    const planets = res.plan[i];
    div$$ = document.createElement('div')
    console.log(planets)
}


